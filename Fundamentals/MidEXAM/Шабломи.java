package bg.softuni.fundamentals.MidEXAM;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

public class Шабломи {
    public static void main(String[] args) {

        // Принтира само числа разделени по ШПАЦИА Лист!!
        // System.out.println(numbers.toString().replaceAll("[\\[\\],]",""));
        //System.out.print(Arrays.toString(new List[]{numbers}).replaceAll("[\\[\\]]", "").replaceAll(", ", "|"));


        //        Принтиране на инт масив!!
//        System.out.println(Arrays.toString(currentState).replaceAll("[\\[\\],]",""));


//        Принтиране до края без символ на края
//        for (int i = 0; i < numbers.size(); i++) {
//            System.out.print(numbers.get(i));
//            if (i < numbers.size() - 1) {
//                System.out.print("|");
//            }
//        }

//        List<Integer> numbers = Arrays.stream(scanner.nextLine().split(" "))
//                .map(Integer::parseInt)
//                .collect(Collectors.toList());

//        int [] currentState = Arrays.stream(scanner.nextLine().split("\\s+"))
//                .mapToInt(Integer::parseInt).toArray();


    }
}
